{
  "name": "soundbuffer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "build": "npm run check && vite build",
    "watch": "vite build --watch"
  },
  "devDependencies": {
    "@types/chrome": "0.2.9",
    "typescript": "7.0.2",
    "vite": "8.3.0"
  }
}
